import 'package:flutter/material.dart';

/// Warm palette of the parent dashboard, taken from the reference
/// ParentDashboardPage (cream background, orange #F5A623 accents).
abstract final class ParentPalette {
  static const creamTop = Color(0xFFFEF8EF);
  static const creamBottom = Color(0xFFF9F4EE);
  static const orange = Color(0xFFF5A623);
  static const orangeDeep = Color(0xFFE8780C);
}
